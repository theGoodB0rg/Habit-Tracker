<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Alarm permission for older API levels -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".HabitTrackerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitTracker"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HabitTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Reminder Engine Components -->
        <receiver
            android:name=".reminders.HabitReminderReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.habittracker.HABIT_REMINDER" />
                <action android:name="com.habittracker.SUMMARY_REMINDER" />
                <action android:name="com.habittracker.SNOOZE_REMINDER" />
                <action android:name="com.habittracker.DISMISS_REMINDER" />
            </intent-filter>
        </receiver>
        
        <receiver
            android:name=".reminders.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
        
        <service
            android:name=".reminders.ReminderNotificationService"
            android:exported="false" />
        
        <!-- Professional Widget Provider (Enhanced) - ACTIVE -->
        <receiver
            android:name="com.habittracker.widget.ProfessionalHabitsWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.habittracker.widget.TOGGLE_HABIT" />
                <action android:name="com.habittracker.widget.REFRESH" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_habits_info" />
        </receiver>
        
        <service
            android:name="com.habittracker.widget.ProfessionalHabitsWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- Timer runtime service (Phase 5) -->
        <service
            android:name=".timing.TimerService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|dataSync|connectedDevice" />
        
        <!-- Unified Timer Action Receiver for widgets/notifications -->
        <receiver
            android:name=".timing.TimerActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.habittracker.timer.action.COORDINATED_START" />
                <action android:name="com.habittracker.timer.action.COORDINATED_PAUSE" />
                <action android:name="com.habittracker.timer.action.COORDINATED_RESUME" />
                <action android:name="com.habittracker.timer.action.COORDINATED_COMPLETE" />
                <action android:name="com.habittracker.timer.action.COORDINATED_EXTEND" />
                <action android:name="com.habittracker.timer.action.COORDINATED_DISCARD" />
            </intent-filter>
        </receiver>
        
    </application>

</manifest>
